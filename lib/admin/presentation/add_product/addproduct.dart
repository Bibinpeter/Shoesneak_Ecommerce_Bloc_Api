import 'package:flutter/material.dart';

class AddProductingScrn extends StatelessWidget {
  const AddProductingScrn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text("adddddddd"),);
  }
}